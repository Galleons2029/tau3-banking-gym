# r10 repair3：补卡参考流程与恢复预算

本版运行目录为 `data/synthesis/targeted-native-v03-r10-repair3`，接续已停止的
repair2，沿用用户确认的完整修复与量产推进范围。历史目录、原始轨迹和审核结论保留。

## 修复依据

repair2 完成 19/20 题准入和 76/80 个计划采样槽位，导出 30 条合格试跑轨迹。
第 9 题准入异议仍阻止进入 400 题阶段，审核未决采样已清零。

检查两道补卡题的四条双模型盲解，发现业务状态及独立义务均正确，官方奖励仍为零。
完整 DB 差异只有 `get_user_dispute_history_7291` 的发现工具调用日志。生成器将销户所需
的争议记录查询放入了补卡参考流程；运行时按参考动作生成只读日志白名单，因此它
意外成为成功条件。公开补卡政策没有此项要求，销户政策才有。

修复只移除补卡参考中的销户查询；销户仍保留它，官方评分器和默认域未修改。
长任务通过增加真实目标卡数量保持原动作桶，不用多余查询虚增长度。
四条历史盲解在移除该参考约束后，均通过未修改的官方评分器。
这项反事实诊断保存在 `verification/existing-capture-scoring-proof.json`，不覆盖原结果。

另一项准入异议将文档中的示意参数名当作强制签名。准入审核现在同时接收盲解中实际
成功解锁返回的公开签名，明确以其参数契约为准；不把隐藏用户工具或私有状态当作公开证据。

连续修复的预算预测也保留祖先试跑的原始成本，防止只用上一版廉价重审的增量外推
量产费用。历史消耗只继承一次，复用轨迹不增加教师采样次数，新请求仍计入累计预算。

## 验证与执行

- native/V2 兼容性回归：214 通过；修改文件只读 Ruff 通过。
- 20/400/200/3000 配额、编译和配对检查全部通过。
- 400 题训练候选与 200 题验证候选完整正反例静态回放：600/600 通过。
- 新测试覆盖补卡可选查询、销户必需查询、公开解锁证据隔离及跨多轮预算继承。

静态探针不是生产准入。生产画像仍从来源绑定的失败报告画像导入，再独立冻结策略与源码。
本轮于 2026-09-19 15:39（Asia/Shanghai）启动持久监督器，依次执行 20 题试跑、
400 题训练包的生成/采样/导出、200 题验证包生成。任务和采样缺口继续显式阻断完成状态。

GLM 与 Gemini 的共享物理请求并发上限为 256。教师使用 GLM-5.3-Flash，用户及
质量审核使用 Gemini-3.5-Flash，双模型盲解保留。每题四个固定 seed，不替换失败样本。
父版累计 5677 次物理调用、152 次教师采样、135920469 token 全部保留。
教师次数上限为 12232，即历史 152 + 本版试跑最多 80 + 正式 12000。

实时状态以 `supervisor.json`、进度文件和预算文件为准。试跑轨迹不混入正式训练数据。
完整 3000 题仍需外部训练和成对评测回执通过门禁；教师成功不等于基座模型提升。
