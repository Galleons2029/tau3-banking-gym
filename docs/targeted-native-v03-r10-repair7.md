# r10 repair7：独立公开解锁证据与跨版本轨迹恢复

## 修复原因

repair6 于 2026-09-19 19:25（UTC+8）停在试跑准入：19/20 题、76/80 个完整采样槽位。
第 6 题的 Gemini 盲解没有使用账户列表工具。审核器把该次盲解中未出现的工具，错误地
视为公开不可解锁。GLM 原始盲解实际已经成功解锁该工具。

独立现场检查确认，该题所需的七个隐藏工具均能通过公开 unlock 包装器解锁，公开文档
均包含对应工具名，且解锁前后 agent/user 业务数据库哈希保持一致。

## 改动与约束

- 私有任务准入审核增加 `independent_public_unlock_probe`：在独立环境中执行公开解锁，
  保存真实返回和来源文档。它只证明相应工具可达，不证明整体业务目标正确。
- 审核器明确区分“该次盲解观察到的解锁结果”和“可用工具集合”。不因为模型漏查询而
  判定任务无效，原有双模型审核、独立目标检查和反例要求继续保留。
- 探针不执行业务查询或写入；结果不加入教师输入，不补写到实际盲解轨迹。
- 允许沿已绑定预算的停止轮次向上寻找已准入的父任务，解决中间修复轮只运行试跑而
  未生成正式任务时无法复用祖先证据的问题。每一层验证 identity、预算和绑定哈希，
  找到任务后仍检查业务、公开环境、模型配置、seed，并严格回放真实工具返回。
- 已改变业务目标的任务仍不复用。原始成功和失败轨迹不被替换；累计预算不清零。
- 新源码使用独立 repair7 快照。repair6 证据及未决判断原样保留。

继续使用 GLM 教师、Gemini 用户/质量审核，总请求并发上限 256，每题四个固定 seed。
阶段顺序为 20 题试跑 → 400 题生成、采样、导出 → 200 题验证包；全量 3000 阶段的
外部训练/评测回执门槛保留。正式阶段单题未决不会阻止其余有效任务采样和导出，
但存在缺口时仍标记不完整，不把它伪装成完整训练包。

## 证据位置

- 原阻塞：`data/synthesis/targeted-native-v03-r10-repair6/tasks/pilot/00006/candidates/0/admission_objection.json`
- 独立探针与回归：`data/synthesis/targeted-native-v03-r10-repair7-preflight/`
- 新轮次：`data/synthesis/targeted-native-v03-r10-repair7/`

启动和运行结果以新轮次的 `launch.json`、`supervisor.json`、`pilot/report.json` 及正式产物为准。

## 2026-09-19 启动检查

135 项回归测试通过，改动文件只读 Ruff 通过。首次新增测试误选了不含隐藏工具的短任务，
已改为真实借记卡争议场景后重新跑完整两组测试，原失败日志保留。

repair7 已启动；其全部任务槽位、配额、四次 seed、分支及成对矩阵与 repair6 一致。
真实恢复检查确认：正式任务 107、111、129 可跨 repair6 找到 repair5 的已准入证据；
业务目标已修正的事故任务 75 被拒绝复用。该检查未调用模型。

原阻塞已实际解除：第 6 题在补齐独立公开证据后，GLM 与 Gemini 均确认有效且公开可解，20/20 题准入通过。只新增两次准入审核，没有重采教师轨迹。绑定证据见新轮 `verification/blocker-resolution.json`。

## 试跑闭环已完成

2026-09-19 21:29（UTC+8）核验：20/20 题有效，80/80 个固定采样槽位完整，41 条合格 SFT；teacher_pass@4 为 70%，sft_yield@4 为 65%。训练清单 READY，预算预测 PASS。独立导出审计检查了 41 个唯一 task/seed 对、725 对 agent 工具调用/返回以及 assistant-only 监督，全部通过。监督器已自动进入正式 400 题的 generate small 阶段，随后继续四次采样、导出和验证包；目前未声称正式批次完成。

## 2026-09-20：固定运行目录恢复正式阶段

上一轮在 2026-09-19 21:30:12 切入正式生成后，初始化时发现共享工作区源码与冻结快照
不一致，退出码为 1；当时尚未实际处理正式任务。试跑通过不等于正式阶段已开始产出。

本次直接从同一轮 `snapshot/sources.json.gz` 恢复 257 个源码文件、716 个语料文件到
`execution/frozen-runtime/`，逐文件核对原快照哈希。没有回退或覆盖共享工作区的修改。
运行配置、训练契约和监督器脚本也复制到独立目录；通过固定 PYTHONPATH、TAU3_DATA_DIR
和工作目录，使后续阶段进程始终使用同一份代码与语料。输出仍写入原 repair7 目录。

预检已验证：138 个加载模块均来自独立目录、公开语料位置正确、原轮次快照/配置/模型
身份校验通过、20 个试跑准入证明可读、正式阶段门槛通过、四个 seed 与 256 并发不变，
新增模型调用为 0、累计预算不变。业务目标有变化的旧任务仍被拒绝复用。

监督器从阶段 3（正式 400 题生成）恢复，不重跑试跑、不清空预算，不更改旧快照以接受
新代码。恢复证明、启动参数、原停止状态和只读观测进程信息位于 `execution/`。实际生成
进度仍以 `train-small-generation-progress.json`、任务证明和请求池记录为准。

### 全局用户文档补齐与实际量产确认

首次隔离启动暴露原快照未覆盖 `data/tau3/user_simulator/` 下的两份全局指导文档。
37 道新盲解在构造用户模拟器时发生缺文件错误。已将这两份未修改的原文件复制到独立
数据根并记录哈希，启动器增加哈希检查；无用户工具、有用户工具两种真实 orchestrator
构造均离线通过，没有调用模型。原错误、预算、seed 和 37 个已建立的截止时间全部保留。

专项恢复进程只选择这类已确认缺文件错误，沿用 `_admit_job` 的原始任务和响应日志恢复。
恢复结束后通过同一 `round.lock` 串行刷新索引、收集仍缺失的固定槽位及导出；已完成槽位
由原 workflow 跳过，避免重复采样。主监督器与恢复进程共享同一请求池和累计预算。
恢复状态在 `execution/asset-recovery.json`，原始错误在 `asset-recovery-inputs.json`。

2026-09-20 03:13（UTC+8）已核验实际产出：主生成进度为 324/400 个槽位已处理，其中
281 VALID、43 INCONCLUSIVE（含正在恢复的 37 个缺文档错误）；扫描任务目录时已有
282 个有效任务证明。新增物理调用 1525 次，活跃请求 105、峰值 246、上限 256。
恢复任务已有 448 个响应日志文件，37 个原截止时间均未改变。轮次 identity 和计划文件
哈希未变。这确认已进入正式量产，不代表 400 题全部完成或正式教师采样已结束。
绑定证明：`execution/production-confirmation.json`。
